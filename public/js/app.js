function ConfirmDelete(){
  var x = confirm("Are you want to delete?");
  if (x)
    return true;
  else
    return false;
}

function ConfirmreturnDelete(){
  var x = confirm("Are you sure?");
  if (x)
    return true;
  else
    return false;
}


function ConfirmAddStatus(){
  var x = confirm("Are you sure?");
  if (x)
    return true;
  else
    return false;
}

function confirmEdit(){
  var x = confirm("คุณต้องการจะแก้ไขใช่หรือไม่ ?");
  if (x)
    return true;
  else
    return false;
}